#include<stdlib.h>
#include "main.h"

/**
 * _abs -  computes the absolute value of an integer.
 *
 * @c: cheks the code
 *
 * Return: the value of the absolut value
 *
 */

int _abs(int c)
{

	int a = abs(c);

	return (a);
}
